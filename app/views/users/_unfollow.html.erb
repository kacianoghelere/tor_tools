<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
						 html: { method: :delete },
						 remote: true) do |f| %>
	<%= f.button type: "submit", class: "btn btn-default btn-xs" do %>
		<%= link_icon('remove') %> Deixar de Seguir
	<% end %>
<% end %>